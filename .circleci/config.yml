version: 2.1

orbs:
  node: circleci/node@2.0.3

commands:
  setup:
    steps:
      - checkout
      - node/install:
          node-version: 12.16.3
          install-yarn: true
      - run:
          name: Versions
          command: |
            node -v
            npm -v
            yarn -v
      - run:
          name: Set env variables
          command: |
            if [ ${CIRCLE_BRANCH} = master ]; then
              echo "export APP_DIR=~/ctodo" >> $BASH_ENV
              source $BASH_ENV
            else
              echo "export APP_DIR=~/stg-ctodo" >> $BASH_ENV
              source $BASH_ENV
            fi
      - node/install-packages:
          app-dir: $APP_DIR
          pkg-manager: yarn
          cache-key: yarn.lock

jobs:
  build:
    executor:
      name: node/default
      tag: 12.16.3
    steps:
      - setup
      - run:
          name: build
          command: yarn build
  deploy:
    executor:
      name: node/default
      tag: 12.16.3
    steps:
      - setup
      - run:
          name: deploy to now
          command: |
            cd ~/project
            yarn deploy:production

workflows:
  version: 2
  now_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
